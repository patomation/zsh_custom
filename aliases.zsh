# List all aliases
alias help="alias"

# Work Github Stuff
alias sdp="git switch develop && git pull"
alias rd="git rebase develop"
alias pr="gh pr create -B develop"
alias g="npm run generate"

# Git
alias switch="git switch $1"

# Reload ZSH
alias r="source ~/.zshrc"